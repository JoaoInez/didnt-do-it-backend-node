type User {
  id: ID!
  username: String!
  email: String!
  password: String!
}

type Query {
  "Finds all users"
  users: [User]!
}

type Mutation {
  "Creates a new user"
  signUp(username: String!, email: String!, password: String!): User
}
